import { FilterFlagsPipe } from './filter-flags.pipe';

describe('FilterFlagsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFlagsPipe();
    expect(pipe).toBeTruthy();
  });
});
